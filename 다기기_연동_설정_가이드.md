# AI 컨설팅 하네스 v7 — 다기기 연동 설정 가이드

**작성일**: 2026-04-25  
**버전**: v7 (61-Agent / 12-Layer)  
**작성**: 중기이코노미기업지원단 영남사업단 경남1본부(진주센터) 전문위원 여운식

---

## 1. 대상 기기

| 기기 | 사용자명 | 비고 |
|------|----------|------|
| 데스크탑 A (집) | — | 마스터 기기 |
| 데스크탑 B (사무실) | Jy | 업로드 원본 기기 |
| 노트북 | sik37 | 현장 컨설팅 기기 |

---

## 2. Google Drive 파일 목록

**폴더명**: AI 컨설팅 하네스 v7  
**폴더 ID**: `1VhkrYjk842q9dGllnw2pyikVnvaeHUrs`  
**폴더 URL**: https://drive.google.com/drive/folders/1VhkrYjk842q9dGllnw2pyikVnvaeHUrs

### 업로드 파일 목록 (총 11개)

| 파일명 (Drive) | 원본 경로 | Drive ID |
|----------------|-----------|----------|
| `CLAUDE.md` | `CLAUDE.md` | `1qfc1z_57VZHHUcmpKlIneD5wmzaOj4nj` |
| `agents__init__.py` | `agents/__init__.py` | `18m1iLTL_6wcEE3_tlysotfV5QQKXmwTy` |
| `agents_verify_finance.py` | `agents/verify_finance.py` | `1nR03HkryIguETWfzYZshCjB2hZIWQfan` |
| `agents_verify_compliance.py` | `agents/verify_compliance.py` | `1ulGk0CGB29UWKWYG_MPAX9NaWOsCqmzf` |
| `agents_data_validation_agent.py` | `agents/data_validation_agent.py` | `1vENoDU4fd33Oyqosv06r1nkCCgvE7HxW` |
| `agents_query_classifier_agent.py` | `agents/query_classifier_agent.py` | `1bhwL-5vmGqmnzufrpVQsus5c_YImi0kG` |
| `agents_risk_score_agent.py` | `agents/risk_score_agent.py` | `1rDAckHJt2RU-dW3b4-xv5yYjjk_LuvMi` |
| `agents_summary_agent.py` | `agents/summary_agent.py` | `1o-bBA15wxd2JLm7G74l3Zx2vlJH4uB-h` |
| `orchestrator_part1_lines1-342.py` | `orchestrator.py` (전반부) | `1ZYQJj4kRR-_Cs-dQ-GDZOUdKCIRqIDnl` |
| `orchestrator_part2_lines343-684.py` | `orchestrator.py` (후반부) | `1U7619OFGLR9I1A83JHL6xmFpNfaMOGkJ` |
| `다기기_연동_설정_가이드.md` | *(본 파일)* | — |

> **⚠ 수동 업로드 필요**  
> `AI_컨설팅_하네스_v7_완성보고서.pptx` (373KB)는 MCP 업로드 한도 초과로  
> Windows 탐색기에서 직접 Google Drive 폴더로 드래그&드롭 필요  
> 원본 경로: `C:\Users\Jy\consulting-agent\AI_컨설팅_하네스_v7_완성보고서.pptx`

---

## 3. 신규 기기 설정 절차

### Step 1 — Python 환경 확인

```powershell
python --version   # 3.11.x 권장
pip --version
```

### Step 2 — 프로젝트 폴더 생성

```powershell
# 데스크탑 A (집): 경로 조정 필요
mkdir C:\Users\<사용자명>\consulting-agent
cd C:\Users\<사용자명>\consulting-agent
mkdir agents
```

### Step 3 — Google Drive에서 파일 다운로드

Google Drive 폴더(위 URL)에서 전체 파일을 로컬 프로젝트 폴더로 다운로드한다.

파일 배치 기준:

```
consulting-agent\
├── CLAUDE.md                          ← CLAUDE.md (Drive 직접 다운로드)
├── orchestrator.py                    ← ⚠ 아래 Step 4 참조 (part1 + part2 합치기)
├── agents\
│   ├── __init__.py                    ← agents__init__.py 이름 변경
│   ├── verify_finance.py              ← agents_verify_finance.py 이름 변경
│   ├── verify_compliance.py           ← agents_verify_compliance.py 이름 변경
│   ├── data_validation_agent.py       ← agents_data_validation_agent.py 이름 변경
│   ├── query_classifier_agent.py      ← agents_query_classifier_agent.py 이름 변경
│   ├── risk_score_agent.py            ← agents_risk_score_agent.py 이름 변경
│   └── summary_agent.py              ← agents_summary_agent.py 이름 변경
```

> Drive에서 `agents_` 접두사 제거 후 원본 파일명으로 복원할 것

### Step 4 — orchestrator.py 합치기 (PowerShell)

Drive에서 `orchestrator_part1_lines1-342.py`와 `orchestrator_part2_lines343-684.py`를  
다운로드한 후 아래 명령으로 하나의 `orchestrator.py`로 합친다.

```powershell
# 다운로드 폴더 → 프로젝트 폴더 기준 (경로 조정)
Get-Content orchestrator_part1_lines1-342.py, orchestrator_part2_lines343-684.py `
  | Set-Content C:\Users\<사용자명>\consulting-agent\orchestrator.py
```

**Linux / Mac (노트북 대비):**

```bash
cat orchestrator_part1_lines1-342.py orchestrator_part2_lines343-684.py \
  > /path/to/consulting-agent/orchestrator.py
```

합치기 검증:

```powershell
(Get-Content orchestrator.py).Count   # 684줄 이어야 함
```

### Step 5 — pip 패키지 설치

```powershell
pip install anthropic --break-system-packages
pip install pptxgenjs          # Node.js 기반 — npm install -g pptxgenjs
pip install markitdown[pptx]
pip install Pillow
```

Node.js 패키지:

```powershell
npm install -g pptxgenjs
```

### Step 6 — API 키 환경변수 등록

```powershell
# PowerShell (현재 세션)
$env:ANTHROPIC_API_KEY = "sk-ant-xxxxxxxxxxxxxxxxxxxxxxxx"

# 영구 등록 (사용자 환경변수)
[System.Environment]::SetEnvironmentVariable(
  "ANTHROPIC_API_KEY",
  "sk-ant-xxxxxxxxxxxxxxxxxxxxxxxx",
  "User"
)
```

> API 키는 https://console.anthropic.com/settings/keys 에서 확인

### Step 7 — CLAUDE.md 경로 확인

Claude Code CLI 실행 전 프로젝트 루트에 `CLAUDE.md`가 존재하는지 확인:

```powershell
Test-Path C:\Users\<사용자명>\consulting-agent\CLAUDE.md
# True 이어야 함
```

### Step 8 — 동작 확인

```powershell
cd C:\Users\<사용자명>\consulting-agent
python -c "from orchestrator import Orchestrator; print('OK')"
```

---

## 4. 기기별 경로 참조표

| 항목 | 데스크탑 B (Jy, 원본) | 데스크탑 A (집) | 노트북 (sik37) |
|------|-----------------------|-----------------|----------------|
| 프로젝트 루트 | `C:\Users\Jy\consulting-agent` | 경로 조정 | 경로 조정 |
| Python | 3.11.9 | 동일 권장 | 동일 권장 |
| Claude Code | CLI (Pro) | CLI (Pro) | CLI (Pro) |
| VS Code | 1.114.0 | — | — |
| GitHub Sync | sik3781-debug | Settings Sync 연동 | Settings Sync 연동 |

---

## 5. agents/ 폴더 나머지 파일 (Drive 미업로드 — 마스터 기기에서 복사)

아래 파일은 Drive에 업로드되지 않았으므로 데스크탑 B(Jy)에서 직접 복사하거나  
`git` 또는 USB를 통해 동기화한다.

- `agents/base_agent.py`
- `agents/tax_agent.py` (TaxAgent)
- `agents/stock_agent.py` (StockAgent)
- `agents/succession_agent.py` (SuccessionAgent)
- `agents/finance_agent.py` (FinanceAgent)
- `agents/legal_agent.py`, `patent_agent.py`, `labor_agent.py`, `real_estate_agent.py`, `insurance_agent.py` (GROUP B)
- GROUP C~I 전체 에이전트 (약 38개 파일)
- `report_agent.py` (PPT 생성)
- `main.py` (진입점)
- `requirements.txt`

> **권장**: 데스크탑 B에서 전체 폴더를 ZIP으로 압축하여  
> Google Drive 또는 USB로 다른 기기에 전달

---

## 6. 전체 ZIP 패키지 생성 (데스크탑 B 실행)

```powershell
Compress-Archive -Path C:\Users\Jy\consulting-agent\* `
  -DestinationPath C:\Users\Jy\Desktop\consulting-agent-v7.zip `
  -Force
```

생성된 `consulting-agent-v7.zip`을 Google Drive에 직접 업로드하거나  
USB로 다른 기기에 전달하면 전체 에이전트 환경이 완전히 복원된다.

---

## 7. 핵심 시스템 참조

| 항목 | 값 |
|------|----|
| 시스템 버전 | v7 |
| 총 에이전트 | 61개 (orchestrator 실행 53개 + 검증 5개 + INFRA 6개 포함) |
| 레이어 | 12개 |
| 실행 패턴 | 직병렬 (레이어 간 직렬 + 레이어 내 병렬) |
| 법인세율 기준 | 2026년 귀속 (10/20/22/25%) |
| 가지급금 인정이자율 | 4.6% (2025년 귀속) |
| 비상장주식 평가 | 순손익:순자산 = 3:2 (일반법인) |

---

*중기이코노미기업지원단 영남사업단 경남1본부(진주센터) | 전문위원 여운식 | 010-2673-3781*
